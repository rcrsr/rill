# Loop Examples
# Demonstrates for-each, while, and annotations

# For-each over list
[1, 2, 3, 4, 5] @ { $ * $ } -> $squares
$squares -> .join(", ") -> log

# For-each over string
"hello" @ { $ } -> $chars
$chars -> .join("-") -> log

# While loop with limit annotation
0 -> $count
^(limit: 100) ($count < 100) @ {
  ($count + 1) -> $count
}
"Final count: {$count}" -> log

# Result
$squares
