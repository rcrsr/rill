{
  "name": "rill-demo-feedback-analyzer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && rill-compose agent.json --target local --output dist/",
    "start": "tsx dist/host.ts",
    "dev": "pnpm build && pnpm start"
  },
  "dependencies": {
    "@rcrsr/rill-compose": "workspace:*",
    "@rcrsr/rill-host": "workspace:*",
    "@rcrsr/rill-ext-openai": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
