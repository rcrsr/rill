{
  "name": "@rcrsr/rill-compose",
  "version": "0.8.6",
  "type": "module",
  "bin": {
    "rill-compose": "./dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint --config ../../eslint.config.js src/",
    "check": "pnpm run build && pnpm run test && pnpm run lint"
  },
  "dependencies": {
    "@rcrsr/rill": "workspace:^",
    "@rcrsr/rill-host": "workspace:^",
    "archiver": "^7.0.1",
    "esbuild": "^0.27.3",
    "zod": "^4.3.6"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  }
}
