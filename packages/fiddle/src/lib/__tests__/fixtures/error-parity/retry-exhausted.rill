# AC-FDL-2: retry<3> exhausted: exactly 3 guard-caught frames, byte-equal
#
# retry<3> catches the type-assertion halt on each attempt.
# After 3 failed attempts the invalid value carries exactly 3
# guard-caught frames (fn === "retry" per recovery.ts convention).

retry<3> { "x" -> :number }
