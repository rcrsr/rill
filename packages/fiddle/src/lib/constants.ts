/**
 * Shared constants for Rill Fiddle
 *
 * Central configuration values used across components and lib modules.
 */

// ============================================================
// EXECUTION CONSTANTS
// ============================================================

/**
 * Default timeout for Rill script execution in milliseconds
 */
export const EXECUTION_TIMEOUT_MS = 5000;

// ============================================================
// LAYOUT CONSTANTS
// ============================================================

/**
 * Minimum panel size in pixels for split pane layout
 */
export const MIN_PANEL_SIZE = 200;

/**
 * Viewport width threshold in pixels for split pane orientation switch
 */
export const DEFAULT_BREAKPOINT = 768;
